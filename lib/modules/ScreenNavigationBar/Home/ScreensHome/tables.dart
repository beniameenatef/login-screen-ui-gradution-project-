import 'package:flutter/material.dart';

class TablesHome extends StatelessWidget {
  const TablesHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text('overview'),),
      ],
    );
  }
}
