import 'package:flutter/material.dart';

class anything extends StatelessWidget {
  const anything({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Center(child: Text('any thing'),),
      ],
    );
  }
}
